{
	"name": "modal",
	"description": "One thing, demanding an answer before anything else. A native `<dialog>`, because the browser already implements the hard parts — the top layer, the backdrop, inertness of everything behind it, and Escape — and a hand-built div gets each of those subtly wrong.",
	"interface": {
		"props": {
			"title": "string",
			"labelledBy": "string"
		},
		"slots": {
			"body": {},
			"actions": {}
		},
		"events": {
			"close": {
				"reason": "string"
			}
		}
	},
	"view": {
		"tag": "dialog",
		"attrs": {
			"aria-labelledby": "$props.labelledBy"
		},
		"$children": [
			{
				"tag": "div",
				"part": "head",
				"$children": [
					{
						"tag": "h2",
						"part": "title",
						"attrs": {
							"id": "$props.labelledBy"
						},
						"text": "$props.title"
					},
					{
						"tag": "button",
						"part": "close",
						"attrs": {
							"type": "button",
							"aria-label": "Close"
						},
						"$icon": {
							"name": "close",
							"size": "1.125rem"
						}
					}
				]
			},
			{
				"tag": "div",
				"part": "body",
				"$children": "body"
			},
			{
				"tag": "div",
				"part": "actions",
				"$children": "actions"
			}
		]
	},
	"styling": {
		"base": {
			"inlineSize": "min(34rem, calc(100vw - 2rem))",
			"maxBlockSize": "calc(100dvh - 4rem)",
			"padding": "0",
			"border": "0",
			"borderRadius": "var(--radius-xl)",
			"background": "var(--color-surface-raised)",
			"color": "var(--color-foreground)",
			"boxShadow": "var(--shadow-overlay)"
		},
		"parts": {
			"backdrop": {
				"$description": "`::backdrop` — the browser's own scrim, so it covers the whole top layer without a fixed-position div that can be scrolled out from under.",
				"background": "var(--color-scrim)"
			},
			"head": {
				"display": "flex",
				"alignItems": "flex-start",
				"justifyContent": "space-between",
				"gap": "var(--space-tight)",
				"padding": "var(--space-loose) var(--space-loose) var(--space-tight)"
			},
			"title": {
				"fontSize": "var(--fs-title)",
				"fontWeight": "600",
				"minInlineSize": "0"
			},
			"close": {
				"flexShrink": "0",
				"display": "inline-flex",
				"alignItems": "center",
				"justifyContent": "center",
				"inlineSize": "2rem",
				"blockSize": "2rem",
				"border": "0",
				"borderRadius": "var(--radius-sm)",
				"background": "transparent",
				"color": "var(--color-foreground-quiet)",
				"cursor": "pointer"
			},
			"body": {
				"padding": "0 var(--space-loose)",
				"overflowY": "auto",
				"color": "var(--color-foreground-soft)"
			},
			"actions": {
				"$description": "Wraps to a column on a narrow viewport rather than shrinking two buttons past their labels.",
				"display": "flex",
				"flexWrap": "wrap",
				"justifyContent": "flex-end",
				"gap": "var(--space-tight)",
				"padding": "var(--space-loose)"
			}
		}
	},
	"a11y": {
		"note": "`showModal()`, never `show()` — only the modal form makes the rest of the document inert and gives Escape its meaning. Focus moves to the dialog on open and RETURNS to the control that opened it on close; a modal that drops focus to the body strands a keyboard user at the top of the page. The destructive action in a confirm dialog wears the SAME danger variant as the trigger that opened it: red in one place and blue in the other is the bug this rule exists to stop."
	}
}
