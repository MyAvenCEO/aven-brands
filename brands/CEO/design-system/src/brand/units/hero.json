{
	"name": "hero",
	"description": "The first screen, as the website actually renders it. A full-bleed stage with media behind it, a scrim over that, and the copy on top \u2014 and it is the one place on any surface where the page's inks do not apply, because text over a photograph has no measurable contrast. Two things make that text readable and both are parts of this unit rather than decisions at a call site: the scrim, and a text-shadow on every line. On the site this was a section with four ids, five hand-written rules and a scrim spelled as three stacked `color-mix` stops.",
	"interface": {
		"props": {
			"labelledBy": "string",
			"poster": "string",
			"src": "string"
		},
		"slots": {
			"media": {
				"accepts": [
					"media"
				]
			},
			"eyebrow": {},
			"heading": {},
			"lead": {},
			"helper": {},
			"actions": {
				"accepts": [
					"btn"
				]
			}
		}
	},
	"view": {
		"tag": "section",
		"attrs": {
			"aria-labelledby": "$props.labelledBy"
		},
		"$children": [
			{
				"tag": "div",
				"part": "media",
				"$children": "media"
			},
			{
				"tag": "div",
				"part": "scrim",
				"attrs": {
					"aria-hidden": "true"
				}
			},
			{
				"tag": "div",
				"part": "content",
				"$children": [
					{
						"tag": "p",
						"part": "eyebrow",
						"$children": "eyebrow"
					},
					{
						"tag": "h1",
						"part": "heading",
						"attrs": {
							"id": "$props.labelledBy"
						},
						"$children": "heading"
					},
					{
						"tag": "p",
						"part": "lead",
						"$children": "lead"
					},
					{
						"tag": "p",
						"part": "helper",
						"$children": "helper"
					},
					{
						"tag": "div",
						"part": "actions",
						"$children": "actions"
					}
				]
			}
		]
	},
	"styling": {
		"base": {
			"$description": "`min-block-size: 85vh` and flex-centred, matching the site exactly. `isolation: isolate` is what lets the media sit at a negative z-index without escaping behind the page.\n\nCONTAINER. This unit establishes an inline-size container named after itself, so anything inside it can respond to the width of THIS BOX rather than to the width of the window. That is the difference that matters for a design system: a composite dropped into a 320px sidebar and the same composite on a 320px phone are the same situation, and a viewport query calls them different ones. The library carries no viewport media query anywhere for exactly this reason \u2014 the only `@media` in it is `prefers-reduced-motion`, which is a person's setting and not a size.\n\n`inline-size: 100%` is the container's REQUIRED companion, not a preference. `container-type: inline-size` makes the box's width independent of its contents \u2014 that is the containment being asked for \u2014 so a container with no width of its own has nothing left to size from and collapses to zero. This unit did: centred in a grid it became a column of single words. A container fills what it is given and bounds itself with `max-inline-size`; it never sizes to its text.",
			"position": "relative",
			"isolation": "isolate",
			"overflow": "hidden",
			"display": "flex",
			"alignItems": "center",
			"justifyContent": "center",
			"minBlockSize": "85vh",
			"paddingInline": "var(--space-comfortable)",
			"paddingBlock": "clamp(5rem, 12vh, 9rem)",
			"color": "var(--color-on-dark)",
			"containerType": "inline-size",
			"containerName": "hero",
			"inlineSize": "100%",
			"minInlineSize": "0"
		},
		"parts": {
			"media": {
				"$description": "Where a `media` unit goes. The hero does not own a video \u2014 it owns a hole that one fits into, which is what lets the same hero sit over a photograph or a loop.",
				"position": "absolute",
				"inset": "0",
				"zIndex": "-2"
			},
			"scrim": {
				"$description": "Stronger at the top and bottom, lighter through the middle where the scene reads \u2014 enough to carry white text, not enough to flatten the picture. Three named stops rather than three `color-mix` calls at a call site.",
				"position": "absolute",
				"inset": "0",
				"zIndex": "-1",
				"background": "linear-gradient(to bottom, var(--color-scrim-strong) 0%, var(--color-scrim) 42%, var(--color-scrim-heavy) 100%)"
			},
			"content": {
				"position": "relative",
				"marginInline": "auto",
				"inlineSize": "100%",
				"maxInlineSize": "48rem",
				"minInlineSize": "0",
				"textAlign": "center"
			},
			"eyebrow": {
				"margin": "0 0 var(--space-tight)",
				"color": "var(--color-on-dark)",
				"$description": "Composes `text--eyebrow`. Only the colour is this unit's; the size, weight, case and tracking come from the one definition."
			},
			"heading": {
				"$description": "`clamp` rather than breakpoints \u2014 a hero headline has one job at every width, and three step changes on the way down is three chances to break a line badly. The text-shadow is not decoration: it is the second half of the contrast guarantee, and it is what keeps the line readable on a frame the scrim alone cannot carry. A heading that names the product should use the LOGO's own wordmark rather than retyping it: `avenCEO` set in the body face beside a display headline is two different avenCEOs on one screen. The `logo` leaf goes inline via `logo--wordmark`, and the size rule below is what makes it sit at the headline's own size instead of the logo's default.",
				"margin": "0",
				"maxInlineSize": "48rem",
				"marginInline": "auto",
				"fontSize": "clamp(2rem, 6.5vw, 4rem)",
				"fontWeight": "300",
				"lineHeight": "1.1",
				"letterSpacing": "var(--tracking-tight)",
				"textWrap": "pretty",
				"color": "var(--color-on-dark)",
				"textShadow": "0 2px 24px var(--color-scrim-text)"
			},
			"lead": {
				"marginBlock": "var(--space-section) 0",
				"marginInline": "auto",
				"maxInlineSize": "42rem",
				"fontSize": "var(--fs-lead)",
				"fontWeight": "300",
				"lineHeight": "1.25",
				"textWrap": "pretty",
				"color": "var(--color-on-dark-soft)",
				"textShadow": "0 1px 16px var(--color-scrim-text)"
			},
			"actions": {
				"display": "flex",
				"flexWrap": "wrap",
				"justifyContent": "center",
				"gap": "var(--space-tight)",
				"marginBlockStart": "var(--space-section)"
			},
			"helper": {
				"$description": "The third line \u2014 quieter still, and the one most often forgotten when a hero is rebuilt by hand.",
				"marginBlock": "var(--space-comfortable) 0",
				"fontSize": "var(--fs-section)",
				"lineHeight": "1.25",
				"color": "var(--color-on-dark-quiet)",
				"textShadow": "0 1px 12px var(--color-scrim-text)"
			},
			"heading-mark": {
				"$description": "The wordmark, inline in the headline. `font-size: inherit` so it grows with the `clamp` above it rather than staying at the logo's own 1.4em, and `vertical-align: baseline` so the two faces sit on one line rather than the display face riding high.",
				"display": "inline-flex",
				"alignItems": "baseline",
				"fontSize": "inherit",
				"verticalAlign": "baseline"
			}
		},
		"variants": {
			"height": {
				"full": {
					"minBlockSize": "100dvh"
				},
				"short": {
					"minBlockSize": "min(56dvh, 34rem)"
				}
			},
			"align": {
				"start": {
					"$part": "content",
					"textAlign": "start"
				}
			},
			"ground": {
				"$description": "A hero without media still needs its own ground, or the light ink lands on the page.",
				"band": {
					"background": "var(--color-band)",
					"color": "var(--color-band-foreground)"
				},
				"marine": {
					"background": "var(--color-primary)",
					"color": "var(--color-primary-foreground)"
				}
			}
		},
		"reducedMotion": {
			"$description": "The video goes and its own poster frame takes its place \u2014 the stage stays, so nothing is lost. Never `display:none` on the media without a ground behind it, or the hero becomes light ink on white.",
			"transition": "none"
		}
	},
	"a11y": {
		"note": "The `<h1>`, once per page, and it is what names the section. The video is decorative: `aria-hidden`, muted, and it must not be the only thing carrying meaning. Under `prefers-reduced-motion` the video is replaced by its poster rather than removed \u2014 removing it would take the ground with it and leave light text on a white page. Contrast here cannot be measured automatically: the scrim is the guarantee, and it is why it is a part of the unit rather than a decision at the call site."
	}
}
