{
	"name": "price-tier",
	"description": "One plan in a set of plans. A price, its terms, and what you get — and the composition rule that four equal tiers are four tiers the eye lands nowhere in, so one of them is `featured` and the rest are quiet. On the site each tier was a card, a hand-built price line and a bare `<ul>` with `space-y-1.5`, repeated per tier with the amount and the caption re-typed at three different sizes.",
	"surface": "site",
	"surfaceNote": "the pricing table",
	"interface": {
		"props": {
			"name": "string",
			"amount": "string",
			"terms": "string"
		},
		"slots": {
			"features": {},
			"action": {
				"accepts": ["btn"]
			},
			"note": {}
		}
	},
	"view": {
		"tag": "div",
		"$children": [
			{
				"tag": "p",
				"part": "name",
				"text": "$props.name"
			},
			{
				"tag": "p",
				"part": "price",
				"$children": [
					{
						"tag": "span",
						"part": "amount",
						"text": "$props.amount"
					},
					{
						"tag": "span",
						"part": "terms",
						"text": "$props.terms"
					}
				]
			},
			{
				"tag": "ul",
				"part": "features",
				"$children": "features"
			},
			{
				"tag": "div",
				"part": "action",
				"$children": "action"
			},
			{
				"tag": "p",
				"part": "note",
				"$children": "note"
			}
		]
	},
	"styling": {
		"base": {
			"display": "flex",
			"flexDirection": "column",
			"gap": "var(--space-tight)",
			"blockSize": "100%",
			"padding": "var(--space-loose)",
			"borderRadius": "var(--radius-lg)",
			"border": "1px solid var(--color-border)",
			"background": "var(--color-surface-raised)",
			"containerType": "inline-size",
			"$description": "CONTAINER. This unit establishes an inline-size container named after itself, so anything inside it can respond to the width of THIS BOX rather than to the width of the window. That is the difference that matters for a design system: a composite dropped into a 320px sidebar and the same composite on a 320px phone are the same situation, and a viewport query calls them different ones. The library carries no viewport media query anywhere for exactly this reason — the only `@media` in it is `prefers-reduced-motion`, which is a person's setting and not a size.\n\n`inline-size: 100%` is the container's REQUIRED companion, not a preference. `container-type: inline-size` makes the box's width independent of its contents — that is the containment being asked for — so a container with no width of its own has nothing left to size from and collapses to zero. This unit did: centred in a grid it became a column of single words. A container fills what it is given and bounds itself with `max-inline-size`; it never sizes to its text."
		},
		"parts": {
			"name": {
				"margin": "0",
				"color": "var(--color-foreground-quiet)",
				"$description": "Composes `text--eyebrow`. Only the colour is this unit's; the size, weight, case and tracking come from the one definition."
			},
			"price": {
				"display": "flex",
				"flexWrap": "wrap",
				"alignItems": "baseline",
				"gap": "0.4rem",
				"margin": "0"
			},
			"amount": {
				"$description": "Tabular, always. Prices in a row that do not share digit widths do not line up, and a set of tiers is read by comparing exactly this number.",
				"fontSize": "var(--fs-display)",
				"fontWeight": "600",
				"letterSpacing": "var(--tracking-tight)",
				"fontVariantNumeric": "tabular-nums",
				"color": "var(--color-foreground)"
			},
			"terms": {
				"fontSize": "var(--fs-meta)",
				"color": "var(--color-foreground-quiet)"
			},
			"features": {
				"display": "grid",
				"gap": "0.4rem",
				"margin": "var(--space-tight) 0 0",
				"padding": "0",
				"listStyle": "none",
				"fontSize": "var(--fs-meta)",
				"lineHeight": "1.45",
				"color": "var(--color-foreground-soft)"
			},
			"feature": {
				"display": "grid",
				"gridTemplateColumns": "auto 1fr",
				"gap": "var(--space-tight)",
				"alignItems": "start"
			},
			"action": {
				"$description": "`margin-block-start: auto` pins it to the bottom, so a row of tiers with different feature counts still lines its buttons up. Without it the eye compares three buttons at three heights instead of three prices.",
				"marginBlockStart": "auto",
				"paddingBlockStart": "var(--space-tight)"
			},
			"note": {
				"margin": "0",
				"fontSize": "var(--fs-micro)",
				"color": "var(--color-foreground-quiet)"
			}
		},
		"variants": {
			"emphasis": {
				"featured": {
					"$description": "ONE tier in the set. Four equal tiers is four tiers the eye lands nowhere in — the lead has to be visibly the lead, or the page has no recommendation and the reader has to do the work.",
					"borderColor": "var(--color-accent)",
					"background": "var(--color-surface-page)",
					"boxShadow": "var(--shadow-raised)"
				},
				"quiet": {
					"background": "var(--color-surface-sunken)",
					"borderStyle": "dashed"
				}
			}
		}
	},
	"a11y": {
		"note": "The features are a real `<ul>`, which is what announces 'list, six items' — a set of `<div>`s reads as running prose and gives no count. The featured tier is not marked by colour alone: it carries a badge saying why, because 'the one with the gold border' is not information a screen reader has."
	}
}
