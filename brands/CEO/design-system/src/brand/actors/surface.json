{
	"name": "surface",
	"description": "A padded, rounded box. Four classes — card, card-sm, well, panel — differed only in radius, padding and ground; card-sm and well shared 3 of their 5 declarations.",
	"interface": {
		"props": {},
		"slots": {
			"content": {}
		}
	},
	"view": {
		"tag": "div",
		"$children": "content"
	},
	"styling": {
		"base": {
			"display": "block",
			"borderRadius": "var(--radius-lg)",
			"border": "1px solid var(--color-border)",
			"background": "var(--color-surface-raised)",
			"padding": "var(--space-loose)",
			"boxShadow": "var(--shadow-raised)",
			"containerType": "inline-size",
			"$description": "CONTAINER. This unit establishes an inline-size container named after itself, so anything inside it can respond to the width of THIS BOX rather than to the width of the window. That is the difference that matters for a design system: a composite dropped into a 320px sidebar and the same composite on a 320px phone are the same situation, and a viewport query calls them different ones. The library carries no viewport media query anywhere for exactly this reason — the only `@media` in it is `prefers-reduced-motion`, which is a person's setting and not a size.\n\n`inline-size: 100%` is the container's REQUIRED companion, not a preference. `container-type: inline-size` makes the box's width independent of its contents — that is the containment being asked for — so a container with no width of its own has nothing left to size from and collapses to zero. This unit did: centred in a grid it became a column of single words. A container fills what it is given and bounds itself with `max-inline-size`; it never sizes to its text.\n\nAn explicit `display`, and it is load-bearing rather than tidy. `container-type: inline-size` has NO EFFECT on an element whose display is `inline` — containment needs a block box. This unit declared no display, so rendered as a `<span>` it was `inline`, its containment was inert, and every `@container` rule inside it silently never matched. The navbar found it: at 318px it was still showing a full desktop nav because the query it was waiting on could not fire."
		},
		"variants": {
			"variant": {
				"raised": {
					"background": "var(--color-surface-raised)"
				},
				"sunken": {
					"background": "var(--color-muted)",
					"boxShadow": "none"
				},
				"plain": {
					"background": "transparent",
					"boxShadow": "none"
				},
				"accent": {
					"$description": "The warm tint, for the option being argued FOR. Carries `accent-foreground` with it: marine on this ground measures 13.01:1 and is correct, but it made the panel read as a blue island in a yellow field. The ramp's own ink is 10.67:1 here, so the panel is one family throughout.",
					"background": "var(--color-accent-surface)",
					"border": "0",
					"boxShadow": "none",
					"color": "var(--color-accent-foreground)"
				},
				"success": {
					"$description": "The cool tint, for the outcome. Marine on it is 12.15:1 — the most headroom any of these grounds has, which suits the one a reader is meant to land on.",
					"background": "var(--color-success-surface)",
					"border": "0",
					"boxShadow": "none"
				},
				"lift": {
					"$description": "A step up from whatever is behind it, rather than a colour of its own — `currentColor` at 7%, so the same class lifts off a dark band and off the page without the caller choosing which. For chips that need an edge without spending a tone on it.",
					"background": "color-mix(in srgb, currentColor 7%, transparent)",
					"border": "0",
					"boxShadow": "none"
				},
				"paper": {
					"$description": "The page's own sunken paper — `surface-sunken`, which is NOT `muted`. The existing `sunken` variant uses muted and the two are a shade apart; a caller reaching for \"the recessed ground\" gets whichever the name suggests and they are different colours. Named for the material rather than the direction so the distinction is visible at the call site.",
					"background": "var(--color-surface-sunken)",
					"border": "0",
					"boxShadow": "none"
				}
			},
			"size": {
				"sm": {
					"borderRadius": "var(--radius-sm)",
					"padding": "var(--space-comfortable)"
				},
				"lg": {
					"borderRadius": "var(--radius-xl)",
					"padding": "var(--space-section)"
				},
				"chip": {
					"$description": "The rounded-xl corner with the loose pad — the proportion a badge wants, between `sm`'s tight box and `lg`'s section-sized one.",
					"borderRadius": "var(--radius-xl)",
					"padding": "var(--space-loose)"
				}
			}
		}
	}
}
