{
	"name": "prose",
	"description": "A run of body copy at a readable measure. The legal pages, the skill descriptions and every lede on the site set their own `max-w-2xl`, `max-w-3xl`, `max-w-md` — six different measures for the same job, none of them derived from the text size they hold. This one is in `ch`, so it stays 65 characters wide whatever the font does.",
	"surface": "site",
	"surfaceNote": "long-form marketing and legal copy",
	"interface": {
		"slots": {
			"content": {}
		}
	},
	"view": {
		"tag": "div",
		"$children": "content"
	},
	"styling": {
		"base": {
			"display": "block",
			"maxInlineSize": "65ch",
			"minInlineSize": "0",
			"fontSize": "var(--fs-body)",
			"lineHeight": "1.6",
			"color": "var(--color-foreground-soft)",
			"textWrap": "pretty",
			"containerType": "inline-size",
			"containerName": "prose",
			"inlineSize": "100%",
			"$description": "CONTAINER. This unit establishes an inline-size container named after itself, so anything inside it can respond to the width of THIS BOX rather than to the width of the window. That is the difference that matters for a design system: a composite dropped into a 320px sidebar and the same composite on a 320px phone are the same situation, and a viewport query calls them different ones. The library carries no viewport media query anywhere for exactly this reason — the only `@media` in it is `prefers-reduced-motion`, which is a person's setting and not a size.\n\n`inline-size: 100%` is the container's REQUIRED companion, not a preference. `container-type: inline-size` makes the box's width independent of its contents — that is the containment being asked for — so a container with no width of its own has nothing left to size from and collapses to zero. This unit did: centred in a grid it became a column of single words. A container fills what it is given and bounds itself with `max-inline-size`; it never sizes to its text.\n\nAn explicit `display`, and it is load-bearing rather than tidy. `container-type: inline-size` has NO EFFECT on an element whose display is `inline` — containment needs a block box. This unit declared no display, so rendered as a `<span>` it was `inline`, its containment was inert, and every `@container` rule inside it silently never matched. The navbar found it: at 318px it was still showing a full desktop nav because the query it was waiting on could not fire.\n\nh1: Clamped against the prose container — a doc column at 40rem and one at 65ch are different rooms for the same title, and `cqi` sizes it to the room. The floor is the display step, so it never drops below the scale.\n\nh2: THE RAMP WAS INVERTED. h2 wore `--fs-section` (14px — one pixel over the 13px body, in a light display face) while h3 wore `--fs-title` (15px): the section titles a reader scans by were the smallest headings on the page, and sat visually BELOW their own subsections. Measured on the framework docs, where every section is an h2. Now `--fs-amount` (24px): 1.85x the body — a heading, not a bolder sentence.",
			"$description-elements": "MARKDOWN, THEMED BY ELEMENT. The framework docs (aven-vibes /docs) arrive as rendered markdown with deliberately classless semantic HTML — the engine's renderMarkdownDoc emits no framework classes precisely so a brand can theme it through element selectors, and this unit is where that theming lives. Each rule is scoped inside `.prose` by native nesting, so a bare <table> elsewhere on the site is untouched. The one deviation from the unit's own measure: `pre` and `table` may exceed 65ch of TEXT because code and columns are not sentences — they keep the box and scroll inside it rather than widening the column (rule: wide content scrolls in its own overflow container, the page never scrolls sideways).",
			"& h1": {
				"fontFamily": "var(--font-display)",
				"fontSize": "clamp(var(--fs-display), 4.5cqi, 2.6rem)",
				"fontWeight": "300",
				"lineHeight": "1.15",
				"letterSpacing": "var(--tracking-tight)",
				"color": "var(--color-foreground)",
				"marginBlock": "0 var(--space-comfortable)"
			},
			"& h2": {
				"fontFamily": "var(--font-display)",
				"fontSize": "var(--fs-amount)",
				"fontWeight": "500",
				"lineHeight": "1.2",
				"color": "var(--color-foreground)",
				"marginBlock": "var(--space-section) var(--space-snug)"
			},
			"& h3": {
				"fontSize": "var(--fs-hero)",
				"fontWeight": "600",
				"color": "var(--color-foreground)",
				"marginBlock": "var(--space-loose) var(--space-tight)"
			},
			"& p": {
				"marginBlock": "0 var(--space-comfortable)"
			},
			"& ul, & ol": {
				"marginBlock": "0 var(--space-comfortable)",
				"paddingInlineStart": "1.5em",
				"display": "grid",
				"gap": "var(--space-hairline)"
			},
			"& a": {
				"color": "var(--color-foreground)",
				"textDecorationColor": "var(--color-accent)",
				"textUnderlineOffset": "0.2em"
			},
			"& strong": {
				"fontWeight": "600",
				"color": "var(--color-foreground)"
			},
			"& code": {
				"fontFamily": "var(--font-mono)",
				"fontSize": "0.875em",
				"background": "var(--color-surface-sunken)",
				"border": "1px solid var(--color-border-soft)",
				"borderRadius": "var(--radius-xs)",
				"paddingInline": "0.3em",
				"paddingBlock": "0.08em",
				"overflowWrap": "anywhere"
			},
			"& pre": {
				"background": "var(--color-surface-sunken)",
				"border": "1px solid var(--color-border-soft)",
				"borderRadius": "var(--radius-md)",
				"padding": "var(--space-comfortable)",
				"marginBlock": "0 var(--space-comfortable)",
				"overflowX": "auto",
				"fontSize": "var(--fs-meta)",
				"lineHeight": "1.55"
			},
			"& pre code": {
				"background": "none",
				"border": "0",
				"padding": "0",
				"fontSize": "inherit",
				"overflowWrap": "normal"
			},
			"& blockquote": {
				"marginBlock": "0 var(--space-comfortable)",
				"marginInline": "0",
				"paddingInlineStart": "var(--space-comfortable)",
				"borderInlineStart": "2px solid var(--color-accent)",
				"color": "var(--color-foreground-quiet)"
			},
			"& table": {
				"inlineSize": "100%",
				"borderCollapse": "collapse",
				"marginBlock": "0 var(--space-comfortable)",
				"fontSize": "var(--fs-meta)"
			},
			"& th": {
				"textAlign": "start",
				"fontWeight": "600",
				"color": "var(--color-foreground)",
				"borderBlockEnd": "1px solid var(--color-border)",
				"padding": "var(--space-tight)"
			},
			"& td": {
				"borderBlockEnd": "1px solid var(--color-border-soft)",
				"padding": "var(--space-tight)",
				"verticalAlign": "top"
			},
			"& hr": {
				"border": "0",
				"borderBlockStart": "1px solid var(--color-border-soft)",
				"marginBlock": "var(--space-loose)"
			}
		},
		"variants": {
			"measure": {
				"$description": "45 to 75 characters is the readable band. `narrow` is for a column beside something else; `wide` is the top of the band and not past it.",
				"narrow": {
					"maxInlineSize": "48ch"
				},
				"wide": {
					"maxInlineSize": "75ch"
				}
			},
			"align": {
				"center": {
					"marginInline": "auto",
					"textAlign": "center"
				}
			},
			"size": {
				"lead": {
					"$description": "The paragraph under a heading. Larger and lighter, and the measure narrows to compensate — a longer line at a larger size is harder to track, not easier.",
					"fontSize": "var(--fs-lead)",
					"fontWeight": "300",
					"lineHeight": "1.45",
					"maxInlineSize": "52ch"
				},
				"fine": {
					"fontSize": "var(--fs-micro)",
					"color": "var(--color-foreground-quiet)"
				}
			}
		}
	},
	"a11y": {
		"note": "The measure IS the accessibility feature — WCAG 1.4.8 puts it at 80 characters and the readable band is tighter than that. `ch` rather than `rem` because the constraint is characters per line, and a user who raises their font size should get fewer words per line, not a wider column."
	}
}
