/** avenYMA's brand guideline page — the same page, this brand's values. */
import { createKitchenSink } from '@myavenceo/aven-vibes/brand'
import { avenYma } from './brand.js'

export const { kitchenSinkView, kitchenSinkCss } = createKitchenSink(avenYma)
