/** avenCEO's brand guideline page — the kit's, bound to this brand. */
import { createKitchenSink } from '@myavenceo/aven-vibes/brand'
import { avenCeo } from './brand.js'

export const { kitchenSinkView, kitchenSinkCss } = createKitchenSink(avenCeo)
