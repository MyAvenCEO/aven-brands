{
	"name": "@myavenceo/aven-brands",
	"private": true,
	"version": "0.1.0",
	"type": "module",
	"workspaces": [
		"apps/*",
		"packages/*"
	],
	"scripts": {
		"dev": "bun run build:packages && bun run --cwd apps/website dev",
		"check": "bun run build:packages && bun run --cwd apps/website check",
		"test": "bun test packages/aven-ceo/tests packages/aven-brandkit/tests",
		"build:packages": "bun run --cwd packages/aven-ceo build && bun run --cwd packages/aven-yma build && bun run brand:generate",
		"build:next": "bun run build:packages && PUBLIC_ID_BASE_URL=https://id.next.aven.ceo bun run --cwd apps/website build && bun run --cwd apps/website audit:build next",
		"build:production": "bun run build:packages && PUBLIC_ID_BASE_URL=https://id.aven.ceo bun run --cwd apps/website build && bun run --cwd apps/website audit:build production",
		"lint": "bunx biome check .",
		"format": "bunx biome format --write .",
		"brand:generate": "bun run --cwd packages/aven-ceo generate && bun run --cwd apps/yma brand:generate",
		"dev:yma": "bun run build:packages && bun run --cwd apps/yma dev",
		"build:yma": "bun run build:packages && bun run --cwd apps/yma build",
		"guidelines": "bun run build:packages && bun ./scripts/render-guidelines.ts"
	},
	"devDependencies": {
		"@biomejs/biome": "2.5.6",
		"@myavenceo/aven-vibes": "^0.3.2",
		"typescript": "6.0.2"
	},
	"engines": {
		"bun": ">=1.3.13"
	},
	"description": "Every brand the company renders, and the one kit that renders them."
}
